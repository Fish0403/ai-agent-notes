# mini-swe-agent 源码总览

mini-swe-agent 是极简 AI 软件工程 agent，核心 agent 类不到 100 行 Python。设计立场：模型能力已经够强，不需要自定义工具和复杂历史处理器——只留一个 bash 工具，模型像人一样在终端敲命令就行。

工具/环境细节 → [01-工具与环境.md](01-工具与环境.md)
Agent 循环与异常细节 → [02-循环与异常.md](02-循环与异常.md)

## 1. 三个设计选择

1. **只要 bash**：不用 tool-calling 接口，任何有指令遵循能力的模型都能跑
2. **无状态 subshell**：每次 `subprocess.run` 都是独立进程，没有 shell session。沙箱化只需把 `subprocess.run` 换成 `docker exec`
3. **线性消息历史**：每一步只是往消息列表追加，轨迹 = 消息历史本身，不需要"从轨迹还原上下文"的翻译层

## 2. 三个 Protocol

Model、Environment、Agent 三者只靠 Protocol（duck typing）耦合，不依赖具体实现：

```python
class Model(Protocol):
    def query(self, messages, **kwargs) -> dict: ...
    def format_observation_messages(self, message, outputs, template_vars) -> list[dict]: ...

class Environment(Protocol):
    def execute(self, action, cwd="") -> dict: ...  # 返回 {"output": ..., "returncode": 0}

class Agent(Protocol):
    def run(self, task, **kwargs) -> dict: ...
```

最小装配（`hello_world.py`）：

```python
agent = DefaultAgent(
    LitellmModel(model_name="anthropic/claude-sonnet-4-5-20250929"),
    LocalEnvironment(),
    **yaml.safe_load(Path("config/default.yaml").read_text())["agent"],
)
agent.run("Write a sudoku game")
```

配置通过 YAML + `recursive_merge` 逐层叠加（默认 YAML → 额外配置文件 → 命令行覆盖）。

## 3. 模板系统

Agent 启动时用 Jinja2 渲染两条消息：`system_template`（系统指令）和 `instance_template`（任务描述）。变量来自 agent 配置、环境信息（uname）、模型配置和外部注入的 task。

工具执行结果通过 `observation_template` 格式化后发回模型。输出超长时自动截断为 head + tail，避免占满上下文。

## 4. 异常驱动的控制流

Agent 循环用异常控制状态，不是返回值：

| 异常 | role | 效果 |
|------|------|------|
| `Submitted` | exit | 退出循环 |
| `LimitsExceeded` | exit | 退出循环 |
| `FormatError` | user | 循环继续，模型下一轮看到错误提示并纠正 |
| `UserInterruption` | user | 循环继续，用户反馈写入上下文 |

`step()` 只有一行：`self.execute_actions(self.query())`。消息的 `role == "exit"` 时循环结束。

## 5. 代码结构

```text
src/minisweagent/
├── agents/          # DefaultAgent + InteractiveAgent（yolo/confirm/human 三种模式）
├── environments/    # LocalEnvironment + DockerEnvironment + extra 沙箱
├── models/          # LitellmModel（toolcall）+ LitellmTextbasedModel（正则解析）
├── config/          # YAML 配置 + Jinja2 模板
└── exceptions.py    # InterruptAgentFlow / Submitted / FormatError / LimitsExceeded
```
